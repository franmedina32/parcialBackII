spring:
  application:
    name: movie-service
  config:
    import: optional:configserver:http://localhost:8888
zipkin:
  base-url: http://localhost:9411/
  enabled: true
exchange:
  movie:
    name: my-movie-exchange
queue:
  movie:
    name: my-movie-queue
